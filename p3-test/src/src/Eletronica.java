package src;

public interface Eletronica {
    // esta interface é descrita na especificação mas o seu funcionamento não, a sua utilidade é questional,
    // ainda assim está criada e é utilizada para verificar se uma conta é ou não online (filtro da alinea 2) (o problema é facilmente
    // resolvido de uma forma melhor sem a interface mas já que é descrita...)
}
